server is [
  host => localhost,
  port => 8080,
  max_connections => 100,
  paths => (/home, /user),
];
database is [
  host => db.local,
  port => 3306,
];
enabled is True;
timeout is 30;
threshold is 50;
config_name is main_config;
paths is (/api, /docs);
max_connections is 200;
api_url is $server_host$;
default_value is $threshold$;